---
interface Props {
	title: string
	content: string
	date: Date
}

const { title, content, date }: Props = Astro.props
---

<div class='thought'>
	<h3>{title}</h3>
	<time datetime={date.toISOString()}>{date.toDateString()}</time>
	<p>{content}</p>
</div>
