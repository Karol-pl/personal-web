---
interface Props {
	title: string
	date: Date
}

const { title, date }: Props = Astro.props
---

<div class='note'>
	<h3 class='text-xl'>{title}</h3>
	<time class='text-sm' datetime={date.toISOString()}>{date.toDateString()}</time>
	<div class='text-base'>
		<slot />
	</div>
</div>
